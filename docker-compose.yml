services:
  onlyoffice:
    image: onlyoffice/documentserver:latest
    container_name: alamait-onlyoffice
    restart: unless-stopped
    ports:
      - "8082:80"
    environment:
      JWT_ENABLED: "true"
      JWT_SECRET: "${ONLYOFFICE_JWT_SECRET:-change-me}"
      JWT_HEADER: "Authorization"
